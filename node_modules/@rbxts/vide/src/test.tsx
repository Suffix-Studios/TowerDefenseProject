import Vide, { context, Provider } from ".";

const theme = context("light");

<frame>
	{theme("dark", () => {
		return <textlabel Text={theme()} />;
	})}
</frame>;

<Provider context={theme} value="dark">
	{() => {
		return <textlabel Text={theme()} />;
	}}
</Provider>;
